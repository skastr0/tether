diff --git a/common/define-grammar.js b/common/define-grammar.js
index efb0b76..568605a 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -111,6 +111,8 @@ module.exports = function defineGrammar(dialect) {
       [$.array_pattern, $.tuple_type],
 
       [$.template_literal_type, $.template_string],
+
+      [$._type_query_member_expression_in_type_annotation],
     ]).concat(
       dialect === 'typescript' ? [
         [$.primary_type, $.type_parameter],
@@ -177,7 +179,7 @@ module.exports = function defineGrammar(dialect) {
           field('arguments', $.arguments),
         )),
         prec('template_call', seq(
-          field('function', choice($.primary_expression, $.new_expression)),
+          field('function', choice($.primary_expression, $.new_expression, $.instantiation_expression)),
           field('arguments', $.template_string),
         )),
         prec('member', seq(
@@ -267,7 +269,13 @@ module.exports = function defineGrammar(dialect) {
       jsx_self_closing_element: $ => prec.dynamic(-1, seq(
         $._jsx_start_opening_element,
         '/>',
       )),
 
+      // JSX text may contain a literal `&`; HTML entities are optional.
+      _jsx_child: ($, previous) => choice(
+        previous,
+        alias('&', $.jsx_text),
+      ),
+
       export_specifier: (_, previous) => seq(
         optional(choice('type', 'typeof')),
@@ -322,8 +330,11 @@ module.exports = function defineGrammar(dialect) {
         seq(
           'export',
           'type',
-          $.export_clause,
-          optional($._from_clause),
+          choice(
+            seq('*', $._from_clause),
+            seq($.namespace_export, $._from_clause),
+            seq($.export_clause, optional($._from_clause)),
+          ),
           $._semicolon,
         ),
         seq('export', '=', $.expression, $._semicolon),
@@ -702,6 +713,7 @@ module.exports = function defineGrammar(dialect) {
           $.private_property_identifier,
           alias($.identifier, $.property_identifier),
         )),
+        optional($.type_arguments),
       ),
       _type_query_call_expression_in_type_annotation: $ => seq(
         field('function', choice(
@@ -726,8 +738,6 @@ module.exports = function defineGrammar(dialect) {
         $.readonly_type,
         $.constructor_type,
         $.infer_type,
-        prec(-1, alias($._type_query_member_expression_in_type_annotation, $.member_expression)),
-        prec(-1, alias($._type_query_call_expression_in_type_annotation, $.call_expression)),
       ),
 
       tuple_parameter: $ => seq(
@@ -782,6 +792,8 @@ module.exports = function defineGrammar(dialect) {
         $.intersection_type,
         $.union_type,
         'const',
+        prec(-1, alias($._type_query_member_expression_in_type_annotation, $.member_expression)),
+        prec(-1, alias($._type_query_call_expression_in_type_annotation, $.call_expression)),
       ),
 
       template_type: $ => seq('${', choice($.primary_type, $.infer_type), '}'),
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index facc6de..8d5c3bb 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -324,6 +324,37 @@ f<T>(x)
       (arguments
         (identifier)))))
 
+==================================
+Generic template calls
+==================================
+
+f<T>`x`
+sql<{ a: string }>`SELECT 1`
+
+---
+
+(program
+  (expression_statement
+    (call_expression
+      (instantiation_expression
+        (identifier)
+        (type_arguments
+          (type_identifier)))
+      (template_string
+        (string_fragment))))
+  (expression_statement
+    (call_expression
+      (instantiation_expression
+        (identifier)
+        (type_arguments
+          (object_type
+            (property_signature
+              (property_identifier)
+              (type_annotation
+                (predefined_type))))))
+      (template_string
+        (string_fragment)))))
+
 ==================================
 Instantiation expressions
 ==================================
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index fb586d8..a22a0bb 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -558,6 +558,12 @@ Type-only Export
 export type { UserType }
 export type { UserType } from "./User.js";
 export type { A, B, C as D } from 'mymodule';
+export type * from './module';
+export type * as Name from './module';
+export type * as default from './module';
+export type * as "string-name" from './module';
+export { type x };
+export { type x as y };
 
 ---
 
@@ -582,7 +588,35 @@ export type { A, B, C as D } from 'mymodule';
         (identifier)
         (identifier)))
     (string
-      (string_fragment))))
+      (string_fragment)))
+  (export_statement
+    (string
+      (string_fragment)))
+  (export_statement
+    (namespace_export
+      (identifier))
+    (string
+      (string_fragment)))
+  (export_statement
+    (namespace_export
+      (identifier))
+    (string
+      (string_fragment)))
+  (export_statement
+    (namespace_export
+      (string
+        (string_fragment)))
+    (string
+      (string_fragment)))
+  (export_statement
+    (export_clause
+      (export_specifier
+        (identifier))))
+  (export_statement
+    (export_clause
+      (export_specifier
+        (identifier)
+        (identifier)))))
 
 =======================================
 Flow Export Types
@@ -2194,6 +2228,91 @@ type Foo = import('node:fs').ReadStream & {
           (property_identifier)
           (formal_parameters))))))
 
+==================================
+Import type in composable position
+==================================
+
+type Entries = import("node:fs").Dirent[];
+type Blocks = readonly import("../core/schemas").ContentBlock[];
+type Effect = import("effect").Effect.Effect<string, never>;
+type Binding = import("../api").ImportClause["namedBindings"];
+type Keys = keyof import("node:fs").Dirent;
+let entries: import("node:fs").Dirent[];
+
+---
+
+(program
+  (type_alias_declaration
+    (type_identifier)
+    (array_type
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))))
+  (type_alias_declaration
+    (type_identifier)
+    (readonly_type
+      (array_type
+        (member_expression
+          (call_expression
+            (import)
+            (arguments
+              (string
+                (string_fragment))))
+          (property_identifier)))))
+  (type_alias_declaration
+    (type_identifier)
+    (member_expression
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))
+      (property_identifier)
+      (type_arguments
+        (predefined_type)
+        (predefined_type))))
+  (type_alias_declaration
+    (type_identifier)
+    (lookup_type
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))
+      (literal_type
+        (string
+          (string_fragment)))))
+  (type_alias_declaration
+    (type_identifier)
+    (index_type_query
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))))
+  (lexical_declaration
+    (variable_declarator
+      (identifier)
+      (type_annotation
+        (array_type
+          (member_expression
+            (call_expression
+              (import)
+              (arguments
+                (string
+                  (string_fragment))))
+            (property_identifier)))))))
+
 =========================
 Import assert
 =========================
