diff --git a/common/define-grammar.js b/common/define-grammar.js
index efb0b76..c06e3e0 100644
--- a/common/define-grammar.js
+++ b/common/define-grammar.js
@@ -111,6 +111,8 @@ module.exports = function defineGrammar(dialect) {
       [$.array_pattern, $.tuple_type],
 
       [$.template_literal_type, $.template_string],
+
+      [$._type_query_member_expression_in_type_annotation],
     ]).concat(
       dialect === 'typescript' ? [
         [$.primary_type, $.type_parameter],
@@ -177,7 +179,7 @@ module.exports = function defineGrammar(dialect) {
           field('arguments', $.arguments),
         )),
         prec('template_call', seq(
-          field('function', choice($.primary_expression, $.new_expression)),
+          field('function', choice($.primary_expression, $.new_expression, $.instantiation_expression)),
           field('arguments', $.template_string),
         )),
         prec('member', seq(
@@ -702,6 +704,7 @@ module.exports = function defineGrammar(dialect) {
           $.private_property_identifier,
           alias($.identifier, $.property_identifier),
         )),
+        optional($.type_arguments),
       ),
       _type_query_call_expression_in_type_annotation: $ => seq(
         field('function', choice(
@@ -726,8 +729,6 @@ module.exports = function defineGrammar(dialect) {
         $.readonly_type,
         $.constructor_type,
         $.infer_type,
-        prec(-1, alias($._type_query_member_expression_in_type_annotation, $.member_expression)),
-        prec(-1, alias($._type_query_call_expression_in_type_annotation, $.call_expression)),
       ),
 
       tuple_parameter: $ => seq(
@@ -782,6 +783,8 @@ module.exports = function defineGrammar(dialect) {
         $.intersection_type,
         $.union_type,
         'const',
+        prec(-1, alias($._type_query_member_expression_in_type_annotation, $.member_expression)),
+        prec(-1, alias($._type_query_call_expression_in_type_annotation, $.call_expression)),
       ),
 
       template_type: $ => seq('${', choice($.primary_type, $.infer_type), '}'),
diff --git a/test/corpus/expressions.txt b/test/corpus/expressions.txt
index facc6de..8d5c3bb 100644
--- a/test/corpus/expressions.txt
+++ b/test/corpus/expressions.txt
@@ -324,6 +324,37 @@ f<T>(x)
       (arguments
         (identifier)))))
 
+==================================
+Generic template calls
+==================================
+
+f<T>`x`
+sql<{ a: string }>`SELECT 1`
+
+---
+
+(program
+  (expression_statement
+    (call_expression
+      (instantiation_expression
+        (identifier)
+        (type_arguments
+          (type_identifier)))
+      (template_string
+        (string_fragment))))
+  (expression_statement
+    (call_expression
+      (instantiation_expression
+        (identifier)
+        (type_arguments
+          (object_type
+            (property_signature
+              (property_identifier)
+              (type_annotation
+                (predefined_type))))))
+      (template_string
+        (string_fragment)))))
+
 ==================================
 Instantiation expressions
 ==================================
diff --git a/test/corpus/types.txt b/test/corpus/types.txt
index fb586d8..b8d542d 100644
--- a/test/corpus/types.txt
+++ b/test/corpus/types.txt
@@ -2194,6 +2194,91 @@ type Foo = import('node:fs').ReadStream & {
           (property_identifier)
           (formal_parameters))))))
 
+==================================
+Import type in composable position
+==================================
+
+type Entries = import("node:fs").Dirent[];
+type Blocks = readonly import("../core/schemas").ContentBlock[];
+type Effect = import("effect").Effect.Effect<string, never>;
+type Binding = import("../api").ImportClause["namedBindings"];
+type Keys = keyof import("node:fs").Dirent;
+let entries: import("node:fs").Dirent[];
+
+---
+
+(program
+  (type_alias_declaration
+    (type_identifier)
+    (array_type
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))))
+  (type_alias_declaration
+    (type_identifier)
+    (readonly_type
+      (array_type
+        (member_expression
+          (call_expression
+            (import)
+            (arguments
+              (string
+                (string_fragment))))
+          (property_identifier)))))
+  (type_alias_declaration
+    (type_identifier)
+    (member_expression
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))
+      (property_identifier)
+      (type_arguments
+        (predefined_type)
+        (predefined_type))))
+  (type_alias_declaration
+    (type_identifier)
+    (lookup_type
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))
+      (literal_type
+        (string
+          (string_fragment)))))
+  (type_alias_declaration
+    (type_identifier)
+    (index_type_query
+      (member_expression
+        (call_expression
+          (import)
+          (arguments
+            (string
+              (string_fragment))))
+        (property_identifier))))
+  (lexical_declaration
+    (variable_declarator
+      (identifier)
+      (type_annotation
+        (array_type
+          (member_expression
+            (call_expression
+              (import)
+              (arguments
+                (string
+                  (string_fragment))))
+            (property_identifier)))))))
+
 =========================
 Import assert
 =========================
