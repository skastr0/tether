# Agent steering

This file is honorary tether (folder host: repo root). It is not project doctrine.

- Doctrine: `root.tether` and collocated `*.tether` / `@tether` comments.
- How to write doctrine: `skills/tether/SKILL.md`.
- Build DAG: `.grok/workflows/build-tether.rhai`.
- Do not add architecture markdown, `docs/`, or other tracked `.md`/`.txt` except the allowlist.
- Other agents work in this tree. Touch only files you own. Commit your own work. Never delete, revert, or stash someone else's files.
- Working directory: this repository (`~/Projects/tether`).
