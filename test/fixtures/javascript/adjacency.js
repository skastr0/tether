// @tether
// @symbol greetJs
// Greeting is a rename of the caller's name, not a template.
export function greetJs(name) {
  // @tether
  // A comment inside a body is not adjacent to greetJs.
  const x = 1
  return name
}
